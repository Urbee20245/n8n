name: Sync fork with upstream`n`non:`n  schedule:`n    # Poll daily for upstream changes, including new n8n releases.`n    - cron: "17 9 * * *"`n  workflow_dispatch:`n`npermissions:`n  contents: write`n`nconcurrency:`n  group: sync-fork`n  cancel-in-progress: false`n`njobs:`n  sync:`n    name: Sync master from n8n-io/n8n`n    runs-on: ubuntu-latest`n    steps:`n      - name: Sync fork`n        run: gh repo sync Urbee20245/n8n --source n8n-io/n8n --branch master`n        env:`n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`n